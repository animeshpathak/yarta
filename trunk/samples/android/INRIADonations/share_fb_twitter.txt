<?xml version="1.0" encoding="utf-8"?>
<!--	// private void onMenuFeedback() {
	// Intent i = new Intent(Intent.ACTION_SEND);
	// i.putExtra(Intent.EXTRA_SUBJECT, getString(R.string.about_mail_subject));
	// i.putExtra(Intent.EXTRA_EMAIL, new String[] { "support@spatialia.com" });
	//
	// i.setType("text/plain");
	// startActivity(Intent.createChooser(i,
	// getString(R.string.about_mail_pick)));
	// }
	//
	// private void onMenuShare() {
	// // Launch the Google+ share dialog with attribution to your app.
	// // Intent shareIntent = new
	// // PlusShare.Builder(this).setType("text/plain")
	// // .setText("Welcome to the Google+ platform.")
	// // .setContentUrl(Uri.parse("https://developers.google.com/+/"))
	// // .getIntent();
	// //
	// // startActivityForResult(shareIntent, 0);
	// }
	//
	// private void onMenuTweet() {
	// String messageToShare = getString(R.string.about_share_message);
	//
	// Intent shareIntent = new Intent(android.content.Intent.ACTION_SEND);
	// shareIntent.setType("text/plain");
	// shareIntent.putExtra(android.content.Intent.EXTRA_TEXT, messageToShare);
	// PackageManager pm = getPackageManager();
	// List<ResolveInfo> activityList = pm.queryIntentActivities(shareIntent,
	// 0);
	//
	// if (!shareUsingApp(activityList, "com.twitter.android.PostActivity",
	// shareIntent)) {
	// String url = "http://twitter.com/home?status="
	// + URLEncoder.encode(messageToShare);
	// Intent intent = new Intent(Intent.ACTION_VIEW, Uri.parse(url));
	// startActivity(intent);
	// }
	// }
	//
	// private void loadChangeLog() {
	// try {
	// String text = "";
	// String line = "";
	// BufferedReader reader = new BufferedReader(new InputStreamReader(
	// getAssets().open("changelog.txt",
	// AssetManager.ACCESS_RANDOM)));
	//
	// while ((line = reader.readLine()) != null) {
	// text += line + "\r\n";
	// }
	//
	// setCtrlText(R.id.changeLog, text);
	// } catch (Exception ex) {
	// }
	// }
	//
	// protected boolean shareUsingApp(List<ResolveInfo> activityList,
	// String componentName, Intent shareIntent) {
	// for (final ResolveInfo app : activityList) {
	// if (app.activityInfo.name.contains(componentName)) {
	// final ActivityInfo activity = app.activityInfo;
	// final ComponentName name = new ComponentName(
	// activity.applicationInfo.packageName, activity.name);
	// shareIntent.addCategory(Intent.CATEGORY_LAUNCHER);
	// shareIntent.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK
	// | Intent.FLAG_ACTIVITY_RESET_TASK_IF_NEEDED);
	// shareIntent.setComponent(name);
	// startActivity(shareIntent);
	// return true;
	// }
	// }
	//
	// return false;
	// } -->